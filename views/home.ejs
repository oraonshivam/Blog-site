<%- include("partials/header") -%> 

<h1>Home</h1>

<p><%= homeContent %> </p>

<!-- <% for (let i = 0; i < posts.length; i++) { %>
  <h1><%= posts[i].title %></h1>
  <p><%= posts[i].content %></p>
<% } %> -->
<!-- the above can be also done as -->

<% posts.forEach(function (elements){  %>
  <h1><%= elements.title %> </h1>
  <p> <%= elements.content.substring(0, 100) + " ..." %> <a href="/posts/<%= elements.title %> ">Read more</a></p>
<% }); %>

<%- include("partials/footer") -%> 